---
version: 1
slug: "docs-index-html"
primary_target: "docs/index.html"
related_targets: ["docs/install.html","docs/troubleshooting.html","docs/faq.html"]
---

# Surface brief: BeaverBuddies Stability Fork site (docs/index.html with install, troubleshooting, faq)

Scope: docs/. Home: Persuade; guides: Read. Audience: pairs building one colony together; many know the Workshop original. Action: install the same build as the host, host or join, report problems usefully. Proof: the real connection panel screenshot, Confirmed/Tested status per fix. Constraints: no site test; release.js shared and byte-identical; every page loads it with the same data-repo/data-asset; fallbacks to /releases/latest; data-release-pinned on the status; connection-panel.png path is embedded by the README; PR -> merge; honest status (1.1.14 not played; 1.1.13 final feature release, played); no version history on player pages. Decisions delegated to the agent.

## Direction contract

THESIS: The Stability Fork is a two-person crosscut saw: two sawyers on one blade pulling in rhythm; out of step, the saw binds. It refuses the SaaS template the old site wore (eyebrows, stat strip, icon tiles, identical card grids).

OWN-WORLD: Blued-steel ground (#161b20, bands #1c2329, plates #252e36, edges #3a454f), fresh-cut pine (#e3c285) for what's solid and played, bark (#4a3322), saw steel (#c7cdd2), competition red (#b8322a) only for Download; host orange and guest blue on the saw's two handles. Zilla Slab headings (self-hosted, OFL), body system-ui. Section markers are log rounds with end grain. Dark only, chosen for evening co-op sessions.

STORY: One look: one colony built together, the fork of thomaswp's BeaverBuddies with invites, panel, cursors and fixes; it replaces the Workshop version; 1.1.14 is fixes only and unplayed, 1.1.13 was the final feature release and played. Then three steps, what you get, invites, the panel, cursors, the scorecard of fixes, how it works, limits.

FIRST VIEWPORT: Left: "BeaverBuddies" small in pine over "Stability Fork" (display), the pitch, the fork/credit line, red Download and outlined Install guide, the Workshop line, the status plate (pinned). Right: an SVG log round in end grain with a two-person crosscut saw across its upper third, orange and blue handles; the blade slides in a slow loop with falling sawdust.

FORM: Crosscut Saw Team, candidate 6 of 7 (seed d6e10c4d). Raises: fixes read as a scorecard, Confirmed in play beside Tested (destination blind: one fixed window of truth); one status line that states the version truth plainly. Signature interaction: the saw pulled in rhythm (motion off under reduced motion).

FINISH: unreviewed and undocumented is unfinished; this build ends with the finish review, the verdict, DESIGN.md, and every shipping raster carrying its provenance
